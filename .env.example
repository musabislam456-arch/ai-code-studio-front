# Only needed for production builds (Vercel). Leave unset for local
# dev — Vite's proxy handles routing to the local backend instead.
# Set this to your deployed Railway backend URL:
VITE_API_URL=https://your-backend.up.railway.app
